# 下载校验时长基准

复现视频 A7MSwdXj86k：YouTube 元数据 61 秒，1080p H.264 画面 61.269533 秒，AAC 音频与 MP4 容器 61.347120 秒。原成品与元数据直接比较，容差 0.305 秒，造成误拒绝；其余编码、帧率、尺寸、比例和 faststart 检查通过。与字幕是否开启无关，失败发生在字幕处理前。

下载源音视频各自实测时长与元数据进行粗粒度完整性检查，容差 max(1 秒, min(2 秒, 元数据时长 × 0.005))。成品按所选源流实测最大时长校验，保留原较严格容差和完整解码检查。字幕仍按实际画面流时长校验。本地压缩与字幕不使用 YouTube 元数据容差。

错误明确列出失败项；时长失败显示预计和实际秒数。不打印媒体地址、Cookie 或令牌。保留已有发布与失败清理机制。

## 验证结果

真实视频修复后通过 1080p 下载、合并、成品完整解码校验和英文人工字幕整理，测试文件位于独立临时目录并已清理；没有发布到用户 Downloads。该视频未继续实际烧录，已有生成样本烧录回归通过。媒体、工作进程、压缩及两类字幕测试 114 项通过；新增整秒元数据、截断拦截和中英文回归在后续 23 项测试中通过。静态、格式与 diff 检查通过。未重新打包、提交、推送或发布，未宣称其他视频的失败均已解决。
